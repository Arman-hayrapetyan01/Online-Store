const initialState = {
  check: false,
  checkProducts: [],
};
const checkoutReducer = (state = initialState, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
};
export default checkoutReducer;
